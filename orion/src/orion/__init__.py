"""ORION: an AI research system built on open-weight models.

Subpackages:
    data     — provenance-tracked data pipeline (filters, dedup, quality, contamination, stats)
    verify   — automated verifiers (math, code, SQL, tool calls)
    train    — SFT / preference / RL training entry points
    evals    — benchmark runners, blind comparison, scorecards
    registry — model versions and the promotion gate
"""

__version__ = "0.1.0.dev0"
